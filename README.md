# Insight Topic Dashboard

This repository now contains only the shared web dashboard used for team editing and live sync.

## Main folder

- `shared-dashboard/` - static web app files and Supabase SQL scripts

## Deploy

Use Netlify with:

- Base directory: `shared-dashboard`
- Build command: *(empty)*
- Publish directory: `.`

## Data backend

Use Supabase and run:

- `shared-dashboard/supabase.sql`
- `shared-dashboard/seed.sql` (optional starter data)
